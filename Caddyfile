yourdomain.com {
  reverse_proxy * next:3000

  handle_path /api* {
    reverse_proxy api:8080
  }

  # (не обязательно) — кэш для ассетов Next
  @nextstatic path /_next/static/*
  header @nextstatic Cache-Control "public, max-age=31536000, immutable"
}